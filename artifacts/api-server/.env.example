NODE_ENV=development
PORT=8080
DATABASE_URL=postgresql://user:password@localhost:5432/dbname
SESSION_SECRET=change-me-to-a-random-string

# Only needed for the AI meeting-minutes parsing feature (routes/meetings-parse.ts).
AI_INTEGRATIONS_OPENAI_BASE_URL=https://api.openai.com/v1
AI_INTEGRATIONS_OPENAI_API_KEY=

# Only needed for the AI press-release feature (routes/noticia.ts). Uses the
# native Anthropic Messages API — get a key at https://console.anthropic.com/
ANTHROPIC_API_KEY=
