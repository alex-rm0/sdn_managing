auto-install-peers=false
strict-peer-dependencies=false

# NODE_ENV=production is set at runtime (for secure cookies etc.), but the
# same env var also reaches the install step on Railway's build — without
# this, pnpm skips devDependencies (vite, typescript, esbuild, and most of
# sdn-app's own UI deps live there), breaking the build.
production=false
